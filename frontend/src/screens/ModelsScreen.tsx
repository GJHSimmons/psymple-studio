import { PlaceholderScreen } from './PlaceholderScreen';

export function ModelsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="workspace"
      title="Models"
      copy="The model dashboard lands in a later wave — card grid, import, and new-model actions."
    />
  );
}
