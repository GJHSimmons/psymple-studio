import { PlaceholderScreen } from './PlaceholderScreen';

export function BuilderScreen() {
  return (
    <PlaceholderScreen
      eyebrow="composite"
      title="Builder"
      copy="The nested canvas — nodes, ports, wires, inspector — lands in the Builder stream."
    />
  );
}
