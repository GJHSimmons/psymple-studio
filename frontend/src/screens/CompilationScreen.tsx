import { PlaceholderScreen } from './PlaceholderScreen';

export function CompilationScreen() {
  return (
    <PlaceholderScreen
      eyebrow="ecosystem → simulable system"
      title="Compilation"
      copy="System ODEs, mappings, and context tabs land in the Screens stream."
    />
  );
}
