# Pinned to a known-good set resolved on Python 3.14 (Windows), 2026-09-04.
# psymple declares only loose lower bounds; we pin exact versions for
# reproducibility (ARCH #17). Regenerate deliberately, not by floating latest.

psymple==1.0.4

# psymple's own dependencies, pinned to the resolved known-good versions.
numpy==2.5.2
scipy==1.18.1
sympy==1.14.0
networkx==3.6.1
matplotlib==3.11.1

# FastAPI service stack.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
